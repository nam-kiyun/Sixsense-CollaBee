<?xml version="1.0" encoding="UTF-8"?>
<html xmlns="http://www.w3.org/1999/xhtml"
    xmlns:ev="http://www.w3.org/2001/xml-events"
    xmlns:w2="http://www.inswave.com/websquare" xmlns:xf="http://www.w3.org/2002/xforms">
    <head meta_screenName="프로젝트 설정 사이드 메뉴">
        <w2:type>COMPONENT</w2:type>
        <w2:buildDate/>
        <w2:MSA/>
        <style>
            /* 선택된 메뉴 스타일 */
            .selected-setting-menu {
                background-color: rgba(255,184,35,0.2) !important;
            }
            
            /* 기본 메뉴 스타일 */
            .setting-menu-item {
                background-color: transparent !important;
                transition: background-color 0.3s ease;
            }
            
            /* 선택되지 않은 메뉴는 투명하게 */
            .setting-menu-item:not(.selected-setting-menu) {
                background-color: transparent !important;
            }
            
            /* 호버 효과 */
            .setting-menu-item:hover {
                background-color: rgba(255,184,35,0.1) !important;
            }
            
            /* 선택된 메뉴의 배경색을 강제로 적용 */
            .setting-menu-item.selected-setting-menu {
                background-color: rgba(255,184,35,0.2) !important;
            }
        </style>
        <xf:model>
            <w2:dataCollection baseNode="map">
                <w2:dataMap baseNode="map" id="dm_settingSideMenu">
                    <w2:keyInfo>
                        <w2:key id="selectedMenu" name="선택된메뉴" dataType="text"/>
                        <w2:key id="projectName" name="프로젝트명" dataType="text"/>
                    </w2:keyInfo>
                    <w2:data use="true">
                        <selectedMenu>detail</selectedMenu>
                        <projectName>2차 프로젝트</projectName>
                    </w2:data>
                </w2:dataMap>
            </w2:dataCollection>
            <w2:workflowCollection/>
        </xf:model>
        <w2:layoutInfo/>
        <w2:publicInfo method=""/>
        <script lazy="false" type="text/javascript"><![CDATA[
scwin.onpageload = function() {
    // 기본적으로 세부사항 선택 상태로 설정
    scwin.changeSelectedMenu("detail");
};

// 세부사항 메뉴 클릭 이벤트
scwin.menu_detail_onclick = function(e) {
    console.log("=== 세부사항 메뉴 클릭 ===");
    scwin.changeSelectedMenu("detail");
    // 부모 창의 메뉴 변경 함수 호출
    scwin.callParentMenuChange("projectSettingDetail");
};

// 액세스 메뉴 클릭 이벤트
scwin.menu_access_onclick = function(e) {
    console.log("=== 액세스 메뉴 클릭 ===");
    scwin.changeSelectedMenu("access");
    // 부모 창의 메뉴 변경 함수 호출
    scwin.callParentMenuChange("projectSettingAccess");
};



// 부모 창의 메뉴 변경 함수 호출
scwin.callParentMenuChange = function(menuType) {
    try {
        // WebSquare에서는 $p.parent()를 사용하여 부모 프레임에 접근
        var parentFrame = $p.parent();
        
        if (parentFrame && parentFrame.scwin && parentFrame.scwin.changeMenu) {
            console.log("부모 changeMenu 함수 호출:", menuType, "($p.parent)");
            parentFrame.scwin.changeMenu(menuType);
        } else if (parent && parent.scwin && parent.scwin.changeMenu) {
            console.log("부모 changeMenu 함수 호출:", menuType, "(parent)");
            parent.scwin.changeMenu(menuType);
        } else if (window.parent && window.parent.scwin && window.parent.scwin.changeMenu) {
            console.log("부모 changeMenu 함수 호출:", menuType, "(window.parent)");
            window.parent.scwin.changeMenu(menuType);
        } else {
            console.error("부모 창의 changeMenu 함수를 찾을 수 없습니다.");
        }
    } catch(e) {
        console.error("부모 창 메뉴 변경 오류:", e);
    }
};

// 선택된 메뉴 스타일 변경
scwin.changeSelectedMenu = function(selectedMenu) {
    try {
        console.log("설정 메뉴 선택 변경 시작:", selectedMenu);
        
        // 모든 메뉴에서 selected-setting-menu 클래스 제거
        grp_detail.removeClass("selected-setting-menu");
        grp_access.removeClass("selected-setting-menu");
        
        // 모든 선택 표시 바 숨기기
        grp_activeBar_detail.hide();
        grp_activeBar_access.hide();
        
        
        // 모든 메뉴의 배경색을 투명하게 강제 설정
        grp_detail.setStyle("background-color", "transparent");
        grp_access.setStyle("background-color", "transparent");
        
        // 선택된 메뉴에만 스타일 적용
        switch(selectedMenu) {
            case "detail":
                grp_detail.addClass("selected-setting-menu");
                grp_detail.setStyle("background-color", "rgba(255,184,35,0.2)");
                grp_activeBar_detail.show();
                break;
            case "access":
                grp_access.addClass("selected-setting-menu");
                grp_access.setStyle("background-color", "rgba(255,184,35,0.2)");
                grp_activeBar_access.show();
                break;
        }
        
        console.log("설정 메뉴 선택 완료:", selectedMenu);
        
        // 데이터맵에도 선택된 메뉴 저장
        dm_settingSideMenu.set("selectedMenu", selectedMenu);
    } catch(e) {
        console.log("설정 메뉴 스타일 변경 오류:", e);
    }
};

// 부모에서 호출할 수 있는 메뉴 업데이트 함수
scwin.updateSelectedMenu = function(menuType) {
    scwin.changeSelectedMenu(menuType);
};

// 프로젝트명 업데이트 함수
scwin.updateProjectName = function(projectName) {
    if (projectName) {
        dm_settingSideMenu.set("projectName", projectName);
    }
};

// 뒤로가기 버튼 클릭
scwin.btn_back_onclick = function(e) {
    console.log("뒤로가기 버튼 클릭됨");
    try {
        // 방법 1: 부모의 closeProjectSettingPopup 함수 직접 호출
        if (window.parent && window.parent.scwin && window.parent.scwin.closeProjectSettingPopup) {
            console.log("부모의 closeProjectSettingPopup 함수 호출");
            window.parent.scwin.closeProjectSettingPopup();
            return; // 성공하면 여기서 끝
        }
        
        // 방법 2: 최상위 부모(projectMainPage)의 closeProjectSettingModal 함수 호출  
        if (window.parent && window.parent.parent && window.parent.parent.scwin && window.parent.parent.scwin.closeProjectSettingModal) {
            console.log("최상위 부모의 closeProjectSettingModal 함수 호출");
            window.parent.parent.scwin.closeProjectSettingModal();
            return; // 성공하면 여기서 끝
        }
        
        // 방법 3: 직접 부모에게 메시지 전송
        if (window.parent) {
            console.log("부모창에 메시지 전송");
            window.parent.postMessage({action: 'closeProjectSettingPopup'}, '*');
        }
        
        // 방법 4: 최상위 부모에게 메시지 전송
        if (window.parent && window.parent.parent) {
            console.log("최상위 부모창에 메시지 전송");
            window.parent.parent.postMessage({action: 'closeProjectSettingPopup'}, '*');
        }
        
        // 방법 5: 강제 닫기 시도
        console.log("강제 닫기 시도");
        setTimeout(function() {
            if (window.close) {
                window.close();
            } else if (window.parent && window.parent.close) {
                window.parent.close();
            } else {
                window.history.back();
            }
        }, 100);
        
    } catch(e) {
        console.error("뒤로가기 실행 오류:", e);
        // 최후의 수단
        try {
            window.history.back();
        } catch(e2) {
            console.error("history.back도 실패:", e2);
        }
    }
};
]]></script>
    </head>
    <body ev:onpageload="scwin.onpageload">
        <!-- 설정 사이드메뉴 전체 컨테이너 -->
        <xf:group 
            style="width:303px;height:100%;background-color:#ffffff;border-right:1px solid #000000;position:relative;padding:0;overflow:hidden;"
            id="grp_settingSideMenu">
            
            <!-- 뒤로가기 버튼 -->
            <xf:group style="padding:0 24px 16px 24px;margin:0;" id="grp_backIcon">
                <xf:trigger 
                    style="display:inline-flex;align-items:center;justify-content:flex-start;background:none;border:none;cursor:pointer;padding:0;font-size:36px;color:#333333;margin:0;"
                    id="btn_backIcon" 
                    type="button" 
                    ev:onclick="scwin.btn_back_onclick">
                    <xf:label style="font-size:36px;color:#333333;font-weight:bold;margin:0;padding:0;"><![CDATA[‹]]></xf:label>
                </xf:trigger>
            </xf:group>
            
            <!-- 설정 메뉴 섹션 -->
            <xf:group style="margin-bottom:20px;padding:0 0 20px 0;" id="grp_settingSection">
                
                <!-- 세부사항 메뉴 -->
                <xf:group 
                    style="padding:16px 24px;margin-bottom:8px;cursor:pointer;background-color:transparent;border-radius:10px;margin:0 16px 8px 16px;position:relative;"
                    id="grp_detail"
                    class="setting-menu-item"
                    ev:onclick="scwin.menu_detail_onclick">
                    
                    <!-- 선택 표시 바 -->
                    <xf:group 
                        style="position:absolute;left:0;top:50%;transform:translateY(-50%);width:3px;height:24px;background-color:#ffb823;"
                        id="grp_activeBar_detail"/>
                    
                    <xf:output style="color:#000000;font-size:20px;font-family:Inter,sans-serif;font-weight:400;margin-left:8px;" id="output_detail">
                        <xf:label><![CDATA[세부사항]]></xf:label>
                    </xf:output>
                </xf:group>
                
                <!-- 액세스 메뉴 -->
                <xf:group 
                    style="padding:16px 24px;margin-bottom:8px;cursor:pointer;background-color:transparent;border-radius:10px;margin:0 16px 8px 16px;position:relative;"
                    id="grp_access"
                    class="setting-menu-item"
                    ev:onclick="scwin.menu_access_onclick">
                    
                    <!-- 선택 표시 바 -->
                    <xf:group 
                        style="position:absolute;left:0;top:50%;transform:translateY(-50%);width:3px;height:24px;background-color:#ffb823;display:none;"
                        id="grp_activeBar_access"/>
                    
                    <xf:output style="color:#000000;font-size:20px;font-family:Inter,sans-serif;font-weight:400;margin-left:8px;" id="output_access">
                        <xf:label><![CDATA[액세스]]></xf:label>
                    </xf:output>
                </xf:group>
                
                
                
            </xf:group>
            
        </xf:group>
    </body>
</html>