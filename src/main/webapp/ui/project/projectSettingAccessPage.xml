<?xml version="1.0" encoding="UTF-8"?>
<html xmlns="http://www.w3.org/1999/xhtml"
    xmlns:ev="http://www.w3.org/2001/xml-events"
    xmlns:w2="http://www.inswave.com/websquare" xmlns:xf="http://www.w3.org/2002/xforms">
    <head meta_screenName="프로젝트 설정 액세스 페이지">
        <w2:type>COMPONENT</w2:type>
        <w2:buildDate/>
        <w2:MSA/>
        <style type="text/css">
        <![CDATA[
        body {
            overflow-y: auto !important;
            height: auto !important;
            min-height: 100vh;
        }
        
        /* 그리드뷰 선택 효과 제거 */
        #grd_projectUsers .w2grid_row_selected {
            background-color: transparent !important;
        }
        
        #grd_projectUsers .w2grid_cell_selected {
            background-color: transparent !important;
        }
        
        #grd_projectUsers .w2grid_row:hover {
            background-color: #f8f9fa !important;
        }
        
        #grd_projectUsers .w2grid_body .w2grid_row_selected,
        #grd_projectUsers .w2grid_body .w2grid_row_selected td,
        #grd_projectUsers .w2grid_body .w2grid_row_selected .w2grid_cell,
        #grd_projectUsers .w2grid_body .w2grid_row.w2grid_row_selected {
            background-color: transparent !important;
            background: transparent !important;
        }
        
        #grd_projectUsers .w2grid_body .w2grid_row_focus,
        #grd_projectUsers .w2grid_body .w2grid_row_focus td,
        #grd_projectUsers .w2grid_body .w2grid_row_focus .w2grid_cell {
            background-color: transparent !important;
            background: transparent !important;
        }
        ]]>
        </style>
        <xf:model>
            <w2:dataCollection baseNode="map">
                <w2:dataMap baseNode="map" id="dm_search">
                    <w2:keyInfo>
                        <w2:key id="searchValue" name="검색어" dataType="text"/>
                        <w2:key id="projectId" name="프로젝트ID" dataType="text"/>
                    </w2:keyInfo>
                    <w2:data use="true">
                        <searchValue></searchValue>
                        <projectId></projectId>
                    </w2:data>
                </w2:dataMap>
                
                <!-- 프로젝트 멤버 목록 데이터 -->
                <w2:dataList baseNode="list" id="dl_projectUsers" repeatNode="map">
                    <w2:columnInfo>
                        <w2:column id="userId" name="사용자ID(이메일)" dataType="text"/>
                        <w2:column id="userName" name="이름" dataType="text"/>
                        <w2:column id="role" name="역할" dataType="text"/>
                    </w2:columnInfo>
                </w2:dataList>
                
                <!-- 검색 결과 데이터 -->
                <w2:dataMap baseNode="map" id="dm_searchResult">
                    <w2:keyInfo>
                        <w2:key id="found" name="검색결과" dataType="boolean"/>
                        <w2:key id="isProjectMember" name="프로젝트멤버여부" dataType="boolean"/>
                        <w2:key id="userId" name="사용자ID" dataType="text"/>
                        <w2:key id="userName" name="이름" dataType="text"/>
                        <w2:key id="email" name="이메일" dataType="text"/>
                        <w2:key id="profileImageSrc" name="프로필이미지" dataType="text"/>
                    </w2:keyInfo>
                    <w2:data use="true">
                        <found>false</found>
                        <isProjectMember>false</isProjectMember>
                        <userId></userId>
                        <userName></userName>
                        <email></email>
                        <profileImageSrc></profileImageSrc>
                    </w2:data>
                </w2:dataMap>
            </w2:dataCollection>
            <w2:workflowCollection>
                <!-- 프로젝트 멤버 목록 조회 -->
                <w2:workflow id="wf_getProjectUsers">
                    <w2:requestInfo>
                        <w2:action id="action_getProjectUsers" type="httpService" url="/InsWebApp/ProjectAccessSettings.pwkjson" method="post">
                            <w2:param id="projectId" type="jsonParam" value="" ref="dm_search.projectId"/>
                        </w2:action>
                    </w2:requestInfo>
                    <w2:responseInfo>
                        <w2:action id="action_getProjectUsers" type="json" root="projectUserVoList">
                            <w2:mapping target="dl_projectUsers"/>
                        </w2:action>
                    </w2:responseInfo>
                </w2:workflow>
                
                <!-- 사용자 검색 -->
                <w2:workflow id="wf_searchUser">
                    <w2:param id="email" type="in" value=""/>
                    <w2:param id="projectId" type="in" value=""/>
                    <w2:requestInfo>
                        <w2:action id="action_searchUser" type="httpService" url="/user/search" method="get">
                            <w2:param id="email" type="urlParam" value=""/>
                            <w2:param id="projectId" type="urlParam" value=""/>
                        </w2:action>
                    </w2:requestInfo>
                    <w2:responseInfo>
                        <w2:action id="action_searchUser" type="json" root="">
                            <w2:mapping target="dm_searchResult"/>
                        </w2:action>
                    </w2:responseInfo>
                </w2:workflow>
                
                <!-- 사용자 초대 -->
                <w2:workflow id="wf_inviteUser">
                    <w2:param id="projectId" type="in" value=""/>
                    <w2:param id="userId" type="in" value=""/>
                    <w2:requestInfo>
                        <w2:action id="action_inviteUser" type="httpService" url="/project/{projectId}/settings/access" method="post">
                            <w2:param id="projectId" type="urlPath" value=""/>
                            <w2:param id="userId" type="jsonParam" value=""/>
                            <w2:param id="role" type="jsonParam" value="editor"/>
                        </w2:action>
                    </w2:requestInfo>
                    <w2:responseInfo>
                        <w2:action id="action_inviteUser" type="json" root=""/>
                    </w2:responseInfo>
                </w2:workflow>
                
                <!-- 권한 변경 -->
                <w2:workflow id="wf_changeRole">
                    <w2:requestInfo>
                        <w2:action id="action_changeRole" type="httpService" url="/project/{projectId}/settings/access" method="post">
                            <w2:param id="projectId" type="urlPath" value=""/>
                            <w2:param id="userId" type="jsonParam" value=""/>
                            <w2:param id="role" type="jsonParam" value=""/>
                        </w2:action>
                    </w2:requestInfo>
                    <w2:responseInfo>
                        <w2:action id="action_changeRole" type="json" root=""/>
                    </w2:responseInfo>
                </w2:workflow>
            </w2:workflowCollection>
        </xf:model>
        <w2:layoutInfo/>
        <w2:publicInfo method=""/>
        <script lazy="false" type="text/javascript"><![CDATA[
// 초기화 플래그 (페이지 로드 시 팝업 방지용)
var isInitializing = false;

scwin.onpageload = function() {
    
    
    // 초기화 시작
    isInitializing = true;
    
    // URL 파라미터에서 project_id 받기
    var projectId = $p.getParameter("project_id");
    
    
    if (projectId) {
        dm_search.set("projectId", projectId);
        // 프로젝트 멤버 목록 조회
        scwin.loadProjectUsers(projectId);
    } else {
        alert("프로젝트 ID가 필요합니다.");
        return;
    }
    
    // 그리드뷰 선택 효과 제거 설정
    setTimeout(function() {
        if (grd_projectUsers && typeof grd_projectUsers.setSelection === 'function') {
            grd_projectUsers.setSelection(false);
        } else if (grd_projectUsers && typeof grd_projectUsers.clearSelection === 'function') {
            grd_projectUsers.clearSelection();
        }
    }, 100);
    
    // 초기화 완료
    isInitializing = false;
};

// 프로젝트 멤버 목록 조회 (간단한 AJAX 방식)
scwin.loadProjectUsers = function(projectId) {
    
    
    try {
        // 간단한 jQuery AJAX 호출
        $.ajax({
            type: "POST",
            url: "/InsWebApp/ProjectAccessSettings.pwkjson",
            data: "projectId=" + encodeURIComponent(projectId),
            contentType: "application/x-www-form-urlencoded; charset=UTF-8",
            dataType: "json",
            success: function(data) {
                
                
                // 데이터리스트 초기화
                dl_projectUsers.removeAll();
                
                // 응답 데이터에서 프로젝트 사용자 목록 추출
                var projectUserList = data.projectUserListVo.projectUserVoList || [];
                
                
                // 데이터리스트에 데이터 추가
                for (var i = 0; i < projectUserList.length; i++) {
                    var user = projectUserList[i];
                    console.log("프로젝트 멤버 추가:", user);
                    var rowIndex = dl_projectUsers.insertRow();
                    dl_projectUsers.setCellData(rowIndex, "userId", user.userId || "");
                    dl_projectUsers.setCellData(rowIndex, "userName", user.userName || "");
                    dl_projectUsers.setCellData(rowIndex, "role", user.role || "");
                }
                
               
                
                if (dl_projectUsers.getTotalRow() === 0) {
                   
                } else {
                   
                }
            },
            error: function(xhr, status, error) {
                
                alert("프로젝트 멤버 목록을 불러오는데 실패했습니다: " + error);
            }
        });
        
    } catch (e) {
        
        alert("프로젝트 멤버 목록을 불러오는데 오류가 발생했습니다: " + e.message);
    }
};

// 검색 버튼 클릭
scwin.btn_search_onclick = function(e) {
    // 직접 input에서 값 가져오기
    var searchValue = input_search.getValue();
    var projectId = dm_search.get("projectId");
    
    console.log("=== 검색 버튼 클릭 ===");
    console.log("searchValue (from input):", searchValue);
    console.log("projectId:", projectId);
    
    if (!searchValue || searchValue.trim() === "") {
        alert("사용자 ID를 입력해주세요.");
        return;
    }
    
    // 이메일 형식 검증
    var emailPattern = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
    if (!emailPattern.test(searchValue.trim())) {
        alert("올바른 이메일 형식을 입력해주세요.");
        return;
    }
    
    // 사용자 검색 실행
    scwin.searchUser(searchValue.trim(), projectId);
};

// 사용자 검색 함수
scwin.searchUser = function(userId, projectId) {
    console.log("=== 사용자 검색 함수 호출 ===");
    console.log("userId:", userId);
    console.log("projectId:", projectId);
    
    try {
        // 직접 AJAX 호출로 변경
        $.ajax({
            type: "GET",
            url: "/InsWebApp/ProjectUserSearch.pwkjson",
            data: {
                userId: userId,
                projectId: projectId
            },
            dataType: "json",
            success: function(data) {
                console.log("=== 검색 API 성공 ===");
                console.log("응답 데이터:", data);
                
                // 검색 결과를 데이터맵에 설정
                dm_searchResult.set("found", data.found || false);
                dm_searchResult.set("isProjectMember", data.isProjectMember || false);
                dm_searchResult.set("userId", data.userId || "");
                dm_searchResult.set("userName", data.userName || "");
                dm_searchResult.set("email", data.email || "");
                dm_searchResult.set("profileImageSrc", data.profileImageSrc || "");
                
                scwin.handleSearchResult(data);
            },
            error: function(xhr, status, error) {
                console.log("=== 검색 API 오류 ===");
                console.log("xhr:", xhr);
                console.log("status:", status);
                console.log("error:", error);
                console.log("responseText:", xhr.responseText);
                alert("사용자 검색에 실패했습니다.");
            }
        });
    } catch (e) {
        console.log("=== 사용자 검색 예외 발생 ===");
        console.log("예외:", e);
        alert("사용자 검색 중 오류가 발생했습니다.");
    }
};





// 검색 결과 처리
scwin.handleSearchResult = function(result) {
    var found = result.found;
    var isProjectMember = result.isProjectMember;
    var email = result.email;
    var userName = result.userName;
    

    
    if (!found) {
        alert("해당 이메일로 등록된 사용자를 찾을 수 없습니다.");
        return;
    }
    
    if (isProjectMember) {
        alert("'" + userName + "' 사용자는 이미 프로젝트 멤버입니다.");
        return;
    }
    
    // 사용자 추가 가능 상태
    
    var confirmMessage = "사용자 '" + userName + "' (" + email + ")을 프로젝트에 초대하시겠습니까?";
    if (confirm(confirmMessage)) {
        var userId = result.userId;
        scwin.inviteUser(userId);
    }
};

// 사용자 추가 버튼 클릭 (이메일 검색 후 사용)
scwin.btn_addUser_onclick = function(e) {
   
    
    alert("이메일을 검색한 후 사용자를 추가할 수 있습니다.\n\n1. 위의 검색창에 이메일을 입력하세요\n2. 검색 버튼을 클릭하세요\n3. 검색 결과에서 사용자를 추가하세요");
};

// 사용자 초대 함수
scwin.inviteUser = function(userId) {
    var projectId = dm_search.get("projectId");
    
    
    
    try {
        // 워크플로우 실행
        var option = {
            id: "wf_inviteUser",
            action: "action_inviteUser",
            success: function(data) {
                
                alert("사용자 초대가 완료되었습니다.\n\n사용자가 초대를 수락하면 프로젝트 멤버로 추가됩니다.");
                
                // 검색창 초기화
                dm_search.set("searchValue", "");
                
                // 멤버 목록 새로고침
                scwin.loadProjectUsers(projectId);
            },
            error: function(xhr, status, error) {
                
                alert("사용자 초대에 실패했습니다.");
            }
        };
        
        // 파라미터 설정
        wf_inviteUser.setParam("projectId", projectId);
        wf_inviteUser.setParam("userId", userId);
        
        // 워크플로우 실행
        $w.executeWorkflow(option);
    } catch (e) {
        
        alert("사용자 초대 중 오류가 발생했습니다.");
    }
};

// GridView 셀 변경 이벤트 핸들러 (역할 변경 처리)
scwin.grd_projectUsers_oncellchange = function(e) {
    // 초기화 중에는 처리하지 않음
    if (isInitializing) return;
    
    // 역할 컬럼이 변경된 경우에만 처리
    if (e.colID === "role") {
        var rowIndex = e.rowIndex;
        var userId = dl_projectUsers.getCellData(rowIndex, "userId");
        var userName = dl_projectUsers.getCellData(rowIndex, "userName");
        var newRole = e.newValue;
        var oldRole = e.oldValue;
        
        
        
        // 역할이 실제로 변경된 경우에만 처리
        if (newRole !== oldRole) {
            scwin.changeUserRole(userId, userName, newRole);
        }
    }
};

// 권한 변경 함수 (GridView에서 호출)
scwin.changeUserRole = function(userId, userName, newRole) {
    
    
    var projectId = dm_search.get("projectId");
    
    // 일단 권한 변경 기능은 추후 구현 예정
    alert("권한 변경 기능은 현재 개발 중입니다.\n" + userName + "의 역할을 " + 
          (newRole === "admin" ? "관리자" : "에디터") + "로 변경하려고 했습니다.");
    
    // 원래 상태로 되돌리기 위해 목록 새로고침
    scwin.loadProjectUsers(projectId);
};

// 동적 사용자 목록 렌더링
scwin.renderUserList = function() {
    
    
    var userListContainer = document.getElementById("grp_userDataList");
    if (!userListContainer) {
        
        return;
    }
    
    // 기존 사용자 목록 초기화
    userListContainer.innerHTML = "";
    
    // 데이터리스트에서 사용자 정보 가져오기
    var userCount = dl_projectUsers.getTotalRow();
    
    
    if (userCount === 0) {
        // 사용자가 없는 경우 메시지 표시
        var noDataDiv = document.createElement("div");
        noDataDiv.style.cssText = "padding:40px;text-align:center;color:#666666;font-size:16px;";
        noDataDiv.innerHTML = "프로젝트에 참여하는 멤버가 없습니다.";
        userListContainer.appendChild(noDataDiv);
        return;
    }
    
    // 각 사용자에 대해 HTML 생성
    for (var i = 0; i < userCount; i++) {
        var userId = dl_projectUsers.getCellData(i, "userId");
        var userName = dl_projectUsers.getCellData(i, "userName");
        var email = dl_projectUsers.getCellData(i, "email");
        var role = dl_projectUsers.getCellData(i, "role");
        
        // 사용자 행 생성
        var userRow = document.createElement("div");
        userRow.className = "user-row";
        userRow.style.cssText = "display:flex;padding:16px 24px;border-bottom:1px solid #e9ecef;align-items:center;";
        
        // 사용자명
        var nameDiv = document.createElement("div");
        nameDiv.style.cssText = "flex:1;text-align:center;font-size:16px;color:#333333;font-family:Inter,sans-serif;";
        nameDiv.textContent = userName;
        
        // 이메일
        var emailDiv = document.createElement("div");
        emailDiv.style.cssText = "flex:2;text-align:center;font-size:16px;color:#666666;font-family:Inter,sans-serif;";
        emailDiv.textContent = email;
        
        // 역할 선택
        var roleDiv = document.createElement("div");
        roleDiv.style.cssText = "flex:1;text-align:center;";
        
        var roleSelect = document.createElement("select");
        roleSelect.style.cssText = "width:100px;height:36px;border:1px solid #ddd;border-radius:6px;padding:0 8px;font-size:14px;font-family:Inter,sans-serif;";
        
        // 관리자 옵션
        var adminOption = document.createElement("option");
        adminOption.value = "admin";
        adminOption.textContent = "관리자";
        if (role === "admin") adminOption.selected = true;
        
        // 에디터 옵션
        var editorOption = document.createElement("option");
        editorOption.value = "editor";
        editorOption.textContent = "에디터";
        if (role === "editor") editorOption.selected = true;
        
        roleSelect.appendChild(adminOption);
        roleSelect.appendChild(editorOption);
        
        // 역할 변경 이벤트 리스너
        roleSelect.addEventListener("change", function(userId, userName) {
            return function(e) {
                var newRole = e.target.value;
                scwin.changeUserRole(userId, userName, newRole);
            };
        }(userId, userName));
        
        roleDiv.appendChild(roleSelect);
        
        // 행에 요소들 추가
        userRow.appendChild(nameDiv);
        userRow.appendChild(emailDiv);
        userRow.appendChild(roleDiv);
        
        userListContainer.appendChild(userRow);
    }
    
    
};

// 그리드뷰 클릭 이벤트 핸들러 - 선택 효과 제거
scwin.grd_projectUsers_oncellclick = function(e) {
    // 클릭 후 선택 효과 제거
    setTimeout(function() {
        if (grd_projectUsers) {
            grd_projectUsers.clearSelection();
        }
    }, 1);
    return false;
};

// X 버튼 클릭 (프로젝트 페이지로 돌아가기)
scwin.btn_close_onclick = function(e) {
    
    
    // 변경사항 확인 (실제로는 폼 데이터 변경 여부 체크)
    if (confirm("설정 페이지를 닫고 프로젝트 페이지로 돌아가시겠습니까?")) {
        try {
            // 부모 창의 메뉴 변경 함수 호출하여 프로젝트 페이지로 이동
            var parentFrame = $p.parent();
            if (parentFrame && parentFrame.scwin && parentFrame.scwin.changeMenu) {
                parentFrame.scwin.changeMenu("project1"); // 또는 현재 프로젝트에 맞는 메뉴
            }
        } catch(e) {
            
        }
    }
};
]]></script>
    </head>
    <body ev:onpageload="scwin.onpageload">
        <!-- 액세스 페이지 컨테이너 -->
        <xf:group style="padding:40px;background-color:#ffffff;min-height:calc(100vh - 80px);height:auto;position:relative;" id="grp_accessContainer">
            
            <!-- X 버튼 (오른쪽 상단) -->
            <xf:trigger 
                style="position:absolute;top:20px;right:20px;width:32px;height:32px;background-color:transparent;border:none;font-size:24px;color:#999999;cursor:pointer;border-radius:50%;display:flex;align-items:center;justify-content:center;"
                id="btn_close" 
                type="button" 
                ev:onclick="scwin.btn_close_onclick">
                <xf:label><![CDATA[✕]]></xf:label>
            </xf:trigger>
            
            <!-- 페이지 제목 -->
            <xf:group style="margin-bottom:32px;" id="grp_pageTitle">
                <xf:output style="font-size:28px;font-weight:600;color:#333333;font-family:Inter,sans-serif;" id="output_pageTitle">
                    <xf:label><![CDATA[액세스 관리]]></xf:label>
                </xf:output>
                <xf:output style="font-size:16px;color:#666666;font-family:Inter,sans-serif;margin-top:8px;display:block;" id="output_pageDesc">
                    <xf:label><![CDATA[프로젝트에 참여하는 사용자들을 관리할 수 있습니다.]]></xf:label>
                </xf:output>
            </xf:group>
            
            <!-- 검색 및 추가 영역 -->
            <xf:group style="background-color:#f8f9fa;border-radius:12px;padding:24px;margin-bottom:32px;" id="grp_searchArea">
                
                <!-- 검색 바 -->
                <xf:group style="display:flex;gap:16px;align-items:center;margin-bottom:20px;" id="grp_searchBar">
                    <xf:input 
                        style="flex:1;max-width:400px;height:48px;border:1px solid #ddd;border-radius:8px;padding:0 16px;font-size:16px;font-family:Inter,sans-serif;"
                        ref="dm_search.searchValue" 
                        id="input_search"
                        placeholder="이메일로 검색"/>
                    
                    <xf:trigger 
                        style="width:80px;height:48px;background-color:#ffb823;border:1px solid #ffb823;border-radius:8px;color:#000000;font-size:16px;font-family:Inter,sans-serif;cursor:pointer;font-weight:500;"
                        id="btn_search" 
                        type="button" 
                        ev:onclick="scwin.btn_search_onclick">
                        <xf:label><![CDATA[검색]]></xf:label>
                    </xf:trigger>
                </xf:group>
                
                
                
                <!-- 사용자 추가 버튼 -->
                <xf:group style="display:flex;justify-content:flex-end;" id="grp_addButton">
                    <xf:trigger 
                        style="width:140px;height:48px;background-color:#ffb823;border:1px solid #ffb823;border-radius:8px;color:#000000s;font-size:16px;font-family:Inter,sans-serif;cursor:pointer;font-weight:500;"
                        id="btn_addUser" 
                        type="button" 
                        ev:onclick="scwin.btn_addUser_onclick">
                        <xf:label><![CDATA[+ 사용자 추가]]></xf:label>
                    </xf:trigger>
                </xf:group>
                
            </xf:group>
            
            <!-- 사용자 목록 영역 (그리드뷰) -->
            <xf:group style="background-color:#ffffff;border:1px solid #e9ecef;border-radius:12px;overflow:hidden;min-height:900px;" id="grp_userListArea">
            
            <!-- 테이블 헤더 -->
            <xf:group style="background-color:#f8f9fa;padding:16px 24px;border-bottom:1px solid #e9ecef;" id="grp_tableHeader">
                <xf:output style="font-size:18px;font-weight:600;color:#333333;font-family:Inter,sans-serif;" id="output_tableTitle">
                    <xf:label><![CDATA[프로젝트 멤버]]></xf:label>
                </xf:output>
            </xf:group>
            
            <!-- 프로젝트 멤버 그리드 -->
            <w2:gridView 
                id="grd_projectUsers" 
                style="width:100%;height:800px;font-family:Inter,sans-serif;" 
                scrollByColumn="false" 
                defaultCellHeight="40" 
                dataList="dl_projectUsers"
                keepDefaultColumnWidth="true"
                visibleRowNum="20"
                useShiftKey="false"
                useCtrlKey="false"
                rowStatusVisible="false"
                ev:oncellchange="scwin.grd_projectUsers_oncellchange"
                ev:oncellclick="scwin.grd_projectUsers_oncellclick">
                
                <w2:caption id="caption1" style="height:0px;"></w2:caption>
                
                <w2:header id="header1" style="height:40px;">
                    <w2:row id="row1" style="height:40px;">
                        <w2:column width="330" inputType="text" style="text-align:center;font-weight:600;background-color:#f8f9fa;" id="column1" value="이름"></w2:column>
                        <w2:column width="389" inputType="text" style="text-align:center;font-weight:600;background-color:#f8f9fa;" id="column2" value="이메일"></w2:column>
                        <w2:column width="323" inputType="text" style="text-align:center;font-weight:600;background-color:#f8f9fa;" id="column3" value="역할"></w2:column>
                    </w2:row>
                </w2:header>
                
                <w2:gBody id="gBody1">
                    <w2:row id="row2" style="height:40px;">
                        <w2:column width="330" inputType="text" style="text-align:center;" id="userName" blockSelect="false" readOnly="true"></w2:column>
                        <w2:column width="389" inputType="text" style="text-align:center;" id="userId" blockSelect="false" readOnly="true"></w2:column>
                        <w2:column width="323" inputType="select" style="text-align:center;" id="role" blockSelect="false" allOption="">
                            <w2:choices>
                                <w2:itemset nodeset="">
                                    <w2:label ref=""></w2:label>
                                    <w2:value ref=""></w2:value>
                                </w2:itemset>
                                <w2:item>
                                    <w2:label><![CDATA[관리자]]></w2:label>
                                    <w2:value><![CDATA[admin]]></w2:value>
                                </w2:item>
                                <w2:item>
                                    <w2:label><![CDATA[에디터]]></w2:label>
                                    <w2:value><![CDATA[editor]]></w2:value>
                                </w2:item>
                            </w2:choices>
                        </w2:column>
                    </w2:row>
                </w2:gBody>
            </w2:gridView>
                    
                </xf:group>
            
        </xf:group>
    </body>
</html>