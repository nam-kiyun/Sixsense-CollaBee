<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper      
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"      
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!--
	@subject     : 버전관리를 위한 Task(업무) 정보 관련 처리를 담당하는 Sql Mapper
	@description : 버전관리를 위한 Task(업무) 정보 관련 처리를 담당하는 Sql Mapper
	@author      : 남기윤
	@since       : 2025/07/01
	@modification
	===========================================================
	DATE              AUTHOR             DESC
	===========================================================
	2025/07/01              남기윤             최초 생성
 -->
<mapper namespace="com.demo.proworks.taskversion">
    <insert id="insertTaskVersion" parameterType="com.demo.proworks.taskversion.vo.TaskVersionVo" useGeneratedKeys="true" keyProperty="taskVersionId">
    	<!-- 버전관리를 위한 Task(업무) 정보를 등록한다. -->	
    		
        INSERT INTO TASKVERSION   /* QueryID : com.demo.proworks.taskversion.insertTaskVersion */
        ( 
            TASK_VERSION_ID
            <if test="taskId        != null"> , TASK_ID         </if>
            <if test="content        != null"> , CONTENT         </if>
            <if test="createdAt     != null"> , CREATED_AT      </if>
        )
        VALUES  
        ( 
            #{taskVersionId}
            <if test="taskId        != null"> , #{taskId}         </if>
            <if test="content        != null"> , #{content}         </if>
            <if test="createdAt     != null"> , #{createdAt}      </if> 
        )          
    </insert>	
	
    <update id="updateTaskVersion" parameterType="com.demo.proworks.taskversion.vo.TaskVersionVo">
    	<!-- 버전관리를 위한 Task(업무) 정보를 갱신한다. -->
    	
        UPDATE TASKVERSION    /* QueryID : com.demo.proworks.taskversion.updateTaskVersion */      
      	  SET  
                TASK_VERSION_ID = #{taskVersionId}        	                       
            <if test="taskId        != null"> , TASK_ID         = #{taskId}         </if>
            <if test="content        != null"> , CONTENT         = #{content}         </if>
            <if test="createdAt     != null"> , CREATED_AT      = #{createdAt}      </if>                 
        WHERE   
                TASK_VERSION_ID = #{taskVersionId}
    </update>
	
    <delete id="deleteTaskVersion" parameterType="com.demo.proworks.taskversion.vo.TaskVersionVo">
    	<!-- 버전관리를 위한 Task(업무) 정보를 삭제한다. -->
    	
        DELETE FROM TASKVERSION   /* QueryID : com.demo.proworks.taskversion.deleteTaskVersion */ 
        WHERE   
                TASK_VERSION_ID = #{taskVersionId}        
    </delete>
	
    <select id="selectTaskVersion" parameterType="com.demo.proworks.taskversion.vo.TaskVersionVo" resultType="com.demo.proworks.taskversion.vo.TaskVersionVo">
    	<!-- 버전관리를 위한 Task(업무) 정보를 상세 조회한다. -->	
        SELECT     /* QueryID : com.demo.proworks.taskversion.selectTaskVersion */
            TASK_VERSION_ID as taskVersionId, TASK_ID as taskId, CONTENT as content, CREATED_AT as createdAt
        FROM TASKVERSION 
        WHERE 
                TASK_ID = #{taskId}
        ORDER BY CREATED_AT DESC
    	LIMIT 1			
    </select>
    
    
    <select id="selectTaskVersionByTaskId" parameterType="com.demo.proworks.taskversion.vo.TaskVersionVo" resultType="com.demo.proworks.taskversion.vo.TaskVersionVo">
    	<!-- 버전관리를 위한 Task(업무) 정보를 상세 조회한다. -->	
        SELECT     /* QueryID : com.demo.proworks.taskversion.selectTaskVersion */
            TASK_VERSION_ID as taskVersionId, TASK_ID as taskId, CONTENT as content, CREATED_AT as createdAt
        FROM TASKVERSION 
        WHERE 
                TASK_ID = #{taskId}
			
    </select>
	
    <select id="selectListTaskVersion" parameterType="com.demo.proworks.taskversion.vo.TaskVersionVo" resultType="com.demo.proworks.taskversion.vo.TaskVersionVo">
    	<!-- 버전관리를 위한 Task(업무) 정보 목록을 조회한다. -->
           SELECT    /* QueryID : com.demo.proworks.taskversion.selectListTaskVersion */
               TASK_VERSION_ID as taskVersionId, TASK_ID as taskId, CONTENT as content, CREATED_AT as createdAt		       
           FROM (
               SELECT  S.*, CEIL( (@ROWNUM:=@ROWNUM+1) / #{pageSize} ) AS PAGE
               FROM (				
                   SELECT 
                       *  		                  
                   FROM TASKVERSION
                   WHERE  1=1    		
	     																														
               ) S, (SELECT @ROWNUM := 0) TMP
           ) A
            WHERE PAGE = #{pageIndex}
    </select>
	
    <select id="selectListCountTaskVersion" parameterType="com.demo.proworks.taskversion.vo.TaskVersionVo" resultType="long">
    <!-- 버전관리를 위한 Task(업무) 정보 목록의 카운트를 조회한다. -->
    
         SELECT    /* QueryID : com.demo.proworks.taskversion.selectListCountTaskVersion */
             COUNT(*) totcnt  		                  
         FROM TASKVERSION
         WHERE  1=1    		

    </select>

</mapper>
